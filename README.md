# CSU Mascot Match

Single-page, serverless mascot quiz for CSU campuses. Pick the right mascot fast, auto-advancing questions, local logo assets, and a shareable score blurb.

Open `index.html` in a browser or serve locally:

```bash
python -m http.server 8000
```
